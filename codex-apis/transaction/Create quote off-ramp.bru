meta {
  name: Create quote off-ramp
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/v1/off-ramp/quote
  body: json
  auth: bearer
}

headers {
  : 
}

auth:bearer {
  token: {{jwt}}
}

body:json {
  {
    "identityId": "id_test1",
    "quoteId": "quote_01JN5M4P5Y5PZTJ2P4Z91D004H",
    "sourceAmount": "1",
    "provider": "",
    "paymentMethodId": "pm_test_1"
  }
}

script:pre-request {
  const jsonwebtoken = require('jsonwebtoken');
  
  const privateKey = bru.getEnvVar("jwt_private_key").replaceAll('\\n', '\n')
  
  const jwt = jsonwebtoken.sign({}, privateKey, {
      issuer: bru.getEnvVar("jwt_issuer"),
      keyid: bru.getEnvVar("jwt_kid"),
      algorithm: 'ES256',
      expiresIn: '3650d', // 10 years
  })
  
  bru.setEnvVar("jwt", jwt)
}
